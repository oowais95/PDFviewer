# Local: mvnw.cmd spring-boot:run  →  http://localhost:${server.port}${server.servlet.context-path}/
server.port=8081
server.servlet.context-path=/pdfViewerWebApp

# Optional: restrict PDF paths to this directory (canonical path on the server). Empty = no restriction.
pdf.root=
